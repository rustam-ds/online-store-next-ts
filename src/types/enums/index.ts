export enum TextVariantTagName {
  h1 = 'h1',
  h2 = 'h2',
  h3 = 'h3',
  p = 'p',
}

export enum Color {
  default = 'default',
  secondary = 'secondary',
  inverted = 'inverted',
}

export enum FontWeight {
  bold = 'bold',
  normal = 'normal',
}

export enum ButtonVariant {
  default = 'default',
  secondary = 'secondary',
}

export enum ProductSize {
  small = 'Small',
  medium = 'Medium',
  large = 'Large',
}
